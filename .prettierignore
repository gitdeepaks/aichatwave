node_modules
.next
out
build
coverage
.turbo

# Generated — reformatting these only creates diff noise, and the tools that
# write them do not preserve it.
pnpm-lock.yaml
drizzle/meta
public/site.webmanifest

# Vendored UI. `components/ui` is shadcn output and `components/ai-elements`
# is AI Elements output; both are re-generated by their CLIs, which would
# revert any formatting on the next `add`. `eslint.config.mjs` already exempts
# `components/ui` from the assertion ban for the same reason — this keeps the
# two tools' idea of "ours" consistent.
#
# `components/ai-elements` is additionally on the chopping block: Phase E
# deletes 44 of its 48 files as unreachable.
components/ui
components/ai-elements
